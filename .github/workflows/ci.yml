name: Automated Linux Backup and Recovery System CI Pipeline


on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubantu-latest

    steps:
    - name: Automated Linux Backup and Recovery System Code
      run: echo "CI is working Successfully"
      
